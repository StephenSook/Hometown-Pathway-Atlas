# Sookra Council — 2026-05-03 Day 7 AM block prioritization

Pressure-test ran on Stephen's proposed 6-8hr next block before Vinh ships
Phase 2 backend. 5 pillar guardians + 5 peer reviewers (hackathon mentor /
USOPC strategy lead / Google Cloud DevRel judge / NYT Upshot data
journalist / B2B SaaS founder).

---

## Chairman synthesis (Section 9 format)

**Problem Statement:** Olympic + Paralympic representation patterns at
US-county-FIPS granularity with strict per-capita parity discipline are
absent from the public web. A parent of an aspiring athlete in Cobb County,
Georgia (or any of ~3,000 US counties) cannot see whether their county's
representation pattern matches anywhere on Team USA's roster, in Olympic OR
Paralympic, OR what the comparable peer counties are.

**The Gap:** STRUCTURAL. USOPC + NFHS + BLS + NWS data is all public —
none has been *joined* at FIPS with parity, because (a) data sources use
different geographic primitives requiring deliberate disambiguation, (b)
parity discipline (Olympic + Paralympic at analytical symmetry) is a cost
incumbents skip, (c) NIL hostility suppresses public athlete-level tooling.
Atlas's "drop names at aggregation step" architecture is the response.

**Key Statistics (with advisor scoring):**
- Move United 63% of 2024 US Paralympic athletes — sourced (Move United
  2024 Impact Report). LANDS at OK leaning FILLER per Pillar 3 advisor;
  abstract noun stack ("community-based adaptive sports chapters") loses
  the body-horror punch StepSafe/Saber bars hit.
- ~50M US children 6-17 (Aspen 2024) — defensible BUT wrong denominator
  per Pillar 5 advisor for Olympic-governance audience; should bridge
  from 835 named Team USA Paris 2024 athletes (USOPC roster, hard) DOWN
  to 50M as funnel.
- Zero existing public county-FIPS Olympic+Paralympic parity tools — 5/10
  unverified absence, 8/10 with gap-analysis screenshot on slide.
- Modeled ~6,000 NGB recruitment positions/yr — labeled "modeled," but
  4/10 as a LEAD number; hard alternative (835 + 50 NGBs) better.
- HeroStat above hero h1 currently shows placeholder "1 in 2" pending
  Vinh task 1.11 — Pillar 3 vacuum, called malpractice on Day 7 by 3
  reviewers.

**Proposed Approach (the 6-8hr block under review):**
1. Sound design plan for Day 9 demo
2. CountyMap editorial polish (hatch + zoom + grid)
3. NotebookLM pre-pitch defensibility oracle pass
4. Pillar 5 number-drift CI check
5. Conditional-phrasing pre-commit grep
6. ComplianceLog cycle timing tune

**Expected Deliverable:** A 6-8hr work block that meaningfully de-risks
the May 11 submission without overlapping Vinh's critical-path Phase 2
backend work. Block must preserve Day 6 gate (already green), conservative
scope (already locked), and the Pillar 4 demo moment (already shipped).

**Before/After:** *Before:* Atlas conservative scope locked, every Day
8-10 deliverable drafted, but pitch lead stat is a placeholder, persona
is demographic-shaped, the structural-gap claim is asserted in copy not
proven on the map face, and Pillar 5 is sourced-but-soft on cost framing.
*After (block executed per chairman reorder):* lead-stat candidates drafted
from existing Phase 1 parquet data, pitch defensibility oracle-passed,
CountyMap shows the four-source provenance footer naming the join, hatch
pattern proves the structural data gap visually, ComplianceLog timing
verified against Beat 4 narration, and CI scripts protect against post-
audit drift.

**Feasibility Assessment:**
- Time: 6-8hr available, items 6+2(scoped)+4+5+3+1 budget at ~5.5hr →
  comfortable
- Tools: Sookra primary stack already in place; husky/lint-staged absent
  (use plain `.githooks/` + git core.hooksPath instead)
- Top 3 risks: (a) CountyMap smooth zoom would stutter on 3000-county
  TopoJSON — CUT, (b) NotebookLM oracle is run-by-Stephen-manually, ship
  the prompt deck not the run, (c) Layer A HeroStat backfill from parquet
  is Vinh-territory technically; Stephen can prep query plan but should
  not crash through Vinh's path

**Council verdict:** **GO with REFRAME on ordering + 2 substantive
additions before execute.**

**Weakest pillar:** **3 (Statistics) — primary**, with secondary risk
on Pillar 5 (per-incident dollar gap, deferred to Day 8 sourcing sprint).
Pillar 1 reviewer flagged WEAK on persona naming, but USOPC-lens reviewer
pushed back: naming a youth-athlete persona ("Maya, 13") flirts with NIL-
spirit risk in Paralympic-pathway framing. Recommended hold: keep current
"kid in Cobb County" persona, sharpen via Q&A prep instead.

**Confidence:** HIGH. 5 independent advisors converge on items 3+6 as
highest-leverage; 5 peer reviewers split convergently between Pillar 3
(HeroStat vacuum) and Pillar 5 (per-incident dollar) as weakest links.

**Flip condition:** If Vinh ships Layer A stat (task 1.11) inside the
6-8hr window, the HeroStat-from-parquet item drops from prep-only to
ship-immediate, and the NotebookLM oracle pass should run AFTER the
real stat is wired in (so the oracle reviews the actual lead, not a
placeholder).

**One thing to do first:** Tune ComplianceLog cycle timing (item 6).
Highest leverage on the locked Pillar 4 demo moment. Fastest to verify
correct (90 min). If timing already correct (T+5s settle vs T+95s Beat
4 narration = 90s buffer), document the math + move to next item.

---

## Convergent claims (where the council agrees)

1. **Items 3 (NotebookLM oracle) and 6 (ComplianceLog timing) are the
   highest-leverage items.** All 5 advisors + 5 reviewers agree. Items 4
   + 5 (CI scripts) are defensive and real but not pitch-changing.
2. **Sound design (item 1) is Day 9 work and should slot LAST or be cut
   from the block.** Multiple reviewers said "draft a plan now is fine
   but does not change demo content."
3. **CountyMap smooth zoom should be CUT.** Pillar 4+7 + NYT Upshot +
   B2B SaaS reviewers all flagged perf risk on 3000-county TopoJSON and
   "low pitch impact for the engineering cost."
4. **Pillar 3 is the weakest pillar Stephen can fix today.** Lead stat
   is filler-leaning, HeroStat is empty, neither current item touches
   either. NotebookLM oracle pass + (optional) Stephen-drafts-3-stats-
   from-parquet are both proposed.
5. **Pillar 5 weak spot is the missing per-incident dollar.** Saber's
   "$13K per misidentified student" is the bar; Atlas's "Zero existing
   tools" is structural not financial. Day 8 sourcing sprint candidate.

## Where the council clashes

1. **Persona naming.** Pillar 1 advisor demanded "Maya, 13, Cobb County,
   searched 'Georgia Paralympic athletes' got ticket sales results." USOPC
   reviewer flagged this as NIL-spirit risk. Hackathon mentor reviewer
   said "do it, you've hit that bar before." **Chairman ruling:** keep
   current "kid in Cobb County" — name a non-athlete proxy ("Stephen's
   own neighborhood" or composite parent figure) only IF the oracle pass
   surfaces persona-specificity weakness AND USOPC/governance language
   can be vetted.
2. **Order of items 4 (Pillar 5 CI) vs 3 (NotebookLM oracle).** Pillar 5
   advisor says 4→3. Pillar 3 advisor says 3 first. **Chairman ruling:**
   3 first because oracle findings inform whether 4 is sufficient or
   whether a Day 8 sourcing sprint is needed. 4 stays in block but
   slot after 3.
3. **Should ComplianceLog get a 2hr "stronger visual shock" upgrade
   (animated diff strikethrough + audio cue)?** Pillar 4 advisor opened
   the question. **Chairman ruling:** NO. ComplianceLog is locked-and-
   shipped at B+. Spending 2hr on visual shock vs spending it on Pillar 3
   (lead stat work) loses. If audio cue lands in sound-design plan
   (item 1) for the Day 9 recording, that's the right place — captured
   audio is more controllable than runtime audio in browser.

## Blind spots the peer review caught

1. **No one in the 5 advisors mentioned actually rehearsing the pitch
   aloud against a stopwatch today.** Hackathon-mentor reviewer caught:
   3:04 script + lead stat + persona never read aloud + timed = cheapest
   30-min diagnostic, makes 60% of this debate moot. **Chairman action:**
   add to Day 8 work (Stephen's CLAUDE.md task list) — first dry run.
2. **Tech Proof beat 4.5 likely "checks the box," doesn't surface enough
   Gemini API depth.** Google Cloud DevRel reviewer caught: needs a
   5-second on-screen JSON schema reveal proving structured output, plus
   adding the Pydantic model verbatim to README. **Chairman action:**
   add to next block (after current 6 items) — README addendum + pitch
   script Beat 4.5 detail.
3. **CountyMap is purely categorical fill — no sequential ramp showing
   per-county metric variance.** NYT Upshot reviewer caught: "the map
   shows selection, not distribution. The project's biggest visual lie."
   **Chairman action:** DEFER until backend Phase 2 exposes per-county
   metrics. Then add sequential ramp as Day 8 / Day 9 work.
4. **No sales motion or pilot path declared in Pillar 5.** B2B SaaS
   founder reviewer caught: state rec depts buy via 9-18mo RFP, no
   freemium tier mentioned, no named-NGB target list (USA Wrestling,
   USA Paralympic Cycling), no time-to-first-dollar honesty. **Chairman
   action:** add to Day 8 sourcing sprint — pitch_pillar5.md addendum
   with sales motion + 2-3 named NGB lighthouse customers.
5. **CountyMap is missing "zoom to your ZIP" affordance.** NYT Upshot
   reviewer caught: user enters ZIP, then hunts for their own pin. Upshot
   would auto-frame. **Chairman action:** add to current block as small
   add (pan source pin to viewport center on results-mount).

## Final recommended order for the 6-8hr block

| # | Item | Time | Why this slot |
|---|------|------|---------------|
| 1 | ComplianceLog cycle timing tune (item 6) | 90 min | Highest leverage on locked Pillar 4 demo. Fastest to verify done. |
| 2 | NotebookLM oracle prompt deck (item 3 prep) | 60 min | Stephen runs the actual oracle pass himself manually. Ship the prompt deck so he can run it any time. |
| 3 | CountyMap polish — hatch + grid + Provenance Footer (item 2 scoped) | 120 min | Structural-gap proof on the map face. SKIP smooth zoom. ADD pan-to-source-pin from peer review. |
| 4 | Pillar 5 number-drift CI script (item 4) | 60 min | Defensive moat after cold-check round 3 caught 3 errors. |
| 5 | Conditional-phrasing pre-commit grep (item 5) | 60 min | Defensive moat for markdown drift. |
| 6 | Sound design plan for Day 9 (item 1) | 60 min | Day 9 work — drafting the plan now is fine. |
| **Total** | | **~7.5hr** | Inside the 8hr ceiling. |

**Two additions outside the block (queued for Day 8):**
- Day 8 morning: Stephen reads pitch_script.md aloud + stopwatches each
  beat. Adjust word counts where over.
- Day 8 sourcing sprint: per-incident dollar number + sales motion +
  2-3 named NGB lighthouse targets — pitch_pillar5.md addendum.

**One addition at risk of becoming a third block addition (Vinh-coordinated):**
- IF Vinh shows up before EOD with shipped Layer A stat → swap HERO_STAT
  constant in `lib/heroStat.ts` + run NotebookLM oracle pass immediately
  on the real lead-stat. Treat this as "drop-everything-and-ship" work.

---

## Anonymization mapping (revealed)

For chain-of-evidence audit only:

| Presented as | Originally | Pillar defended |
|--------------|------------|-----------------|
| Response A | 4th dispatched | Pillar 4+7 (Inevitability + Feasibility) |
| Response B | 1st dispatched | Pillar 1 (Specificity) |
| Response C | 5th dispatched | Pillar 5 (Business Numbers) |
| Response D | 2nd dispatched | Pillar 2 (Structural Gap) |
| Response E | 3rd dispatched | Pillar 3 (Statistics) |

Reviewers were not shown this mapping during their review.

---

## Honest limit on this council

5 Claude personas inside one Claude instance. Prompt-style diversity, not
cross-model diversity. For highest-stakes downstream decisions (e.g.
"should we cut Layer C entirely if backend slips past Day 8 EOD?"), also
run through ChatGPT + Gemini manually and compare — same protocol Section
10 of the Sookra Methodology recommends.

The strongest signal here is convergent: 5 advisors + 5 reviewers
independently flagged Pillar 3 + Pillar 5 as weakest links. That signal
is robust even with same-model fingerprint.
